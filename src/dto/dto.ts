import { ReactNode } from "react";

export type LayoutProps = {
    children: ReactNode;
};

export type Pokemon = {
    name: string;
    url: string;
}
export type PokemonType = {
    name: string;
    url: string;
}